# knowledge/REPORTS.md — 自进化更新日志

> 任务结束（含中断/失败）后，只要知识库发生了更新，就按格式在此追加一行。
> 没有更新 / 判断无参考价值而未写入的任务，**不需要**在此记录（对话中报告"无需更新"即可）。
> 只记录已验证事实，禁止编造；最新日志追加在末尾。

## 追加格式

每行一条：`- YYYY-MM-DD <应用/主题>：<改动摘要>（<验证方式>）`

## 日志

